package io.nuls.network.message;

import io.nuls.network.entity.param.NetworkParam;

public class ByeMessage extends NetworkMessage {

    public ByeMessage(NetworkParam network) {
        super(network);
    }
}
